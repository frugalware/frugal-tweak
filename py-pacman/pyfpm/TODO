*can install a group (menu to treeview)
*can install/remove "some" packages (not one by one) memorize checkbox with 2 lists 
*add a configuration window for edit ~/.pyfpm
*fix utf8 support for read ChangeLog
